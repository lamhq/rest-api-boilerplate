/**
 * Represents the direction in which to sort a collection of items.
 */
export enum SortDirection {
  /**
   * Indicates that the collection should be sorted in ascending order.
   */
  Asc = 'ASC',

  /**
   * Indicates that the collection should be sorted in descending order.
   */
  Desc = 'DESC',
}
